@model CartViewModel
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@if (Model != null && Model.Items.Any())
{
    <h3>Detalle del carrito</h3>
        <ul>
            @foreach (var p in Model.Items)
            {
                <li>
                    @p.ProductName, @(p.UnitPrice ?? 0)
                </li>

            }
        </ul>

@*<table class="table">
    <thead>
        <tr>
            <th>
                Producto
            </th>
            <th>
                Precio
            </th>
        </tr>
    </thead>
    <tbody>

    </tbody>
</table>*@
        <p>Cantidad <strong>@Model.Count</strong></p>
        <p>Total <strong>@Model.Total</strong></p>
        <a class="btn-primary" asp-controller="Home" asp-action="Index">Comprar</a>

        }
        else
        {
        <h3>Su carrito está vacío!</h3>
        }

        <a class="btn-primary" asp-controller="Home" asp-action="Index">Regresar al Listado</a>
